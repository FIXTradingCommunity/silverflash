# Configuration for SellSide with UDP transport
# Uses a series of ports from default base.
protocol=udp
multiplexed=false

# Uses a dedicated dispatcher thread for each session.
# Beyond a certain number of sessions (to be determined by testing),
# an IOReactor event demultiplexor would be beneficial.
reactive=false
clients=2